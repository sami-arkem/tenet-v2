# Pack Integration

## Goal
Make future domain and jurisdiction packs first-class inputs to the real deterministic audit flow.

## What this must do
- enrich audit planning with pack-derived regimes
- enrich audit planning with pack-derived controls
- enrich retrieval with pack-derived query seeds
- keep deterministic core authoritative
- not break existing gold cases

## Rules
- packs expand the audit brain
- packs do not replace audit logic
- pack integration must remain deterministic
